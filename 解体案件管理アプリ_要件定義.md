# 解体案件管理アプリ 要件定義（Claude Code 開発用）

## 1. 概要
解体工事の案件を、問い合わせ受付から完工まで一気通貫で管理するためのアプリ。
既存のExcel等は使っておらず、ゼロから使いやすい専用ツールを作りたい。

- **利用者**：営業管理担当 1名のみ（複数ユーザー対応・権限管理は不要）
- **主な利用端末**：iPhone（Safari中心）。PCでの利用は想定しなくてよい
- **開発方針**：まずは動くベータ版を素早く作り、使いながら育てる想定

## 2. 業務フロー（ステータス）
案件は以下7ステータスを順番に遷移する。

1. 見積もり依頼受付（顧客から問い合わせが来た）
2. 業者へ依頼中（解体業者に見積もりを依頼した）
3. 現地立会い待ち
4. 金額回答済み（顧客へ金額を提示した）
5. 着工待ち（顧客からの着工GOを待っている）
6. 着工中
7. 完工

- ステータスは手動で切り替える想定（自動遷移ロジックは不要）
- 各案件は常にどれか1つのステータスに属する

## 3. 案件データ項目
| 項目 | 型 | 備考 |
|---|---|---|
| 現場名／顧客名 | 文字列 | 例：田中様邸 木造2階解体 |
| 現場住所 | 文字列 | |
| 顧客連絡先 | 文字列 | 電話番号等 |
| 対応業者名・連絡先 | 文字列 | 業者マスタは持たない。案件ごとにその都度手入力 |
| **注文住宅営業担当者名** | 文字列 | 紹介元（ハウスメーカー等）の営業担当者名。**新規追加項目** |
| 現地立会い予定日時 | 日付/文字列 | |
| 着工予定日 | 日付 | |
| 完工予定日 | 日付 | |
| 金額 | 文字列 or 数値 | 合計額のみを記録するメモ的項目。原価・売上を分けた損益計算は不要 |
| ステータス | enum | 上記7種のいずれか |
| 備考 | テキスト | 自由記述 |

- 写真・書類添付は現時点では要件に含めない（将来検討）

## 4. 画面構成
### 4.1 案件一覧画面
- 画面上部：ステータス名のタブ（横スクロール）。各タブに該当件数を表示
- タブ選択で、そのステータスの案件がカード形式で縦に並ぶ
- カードに表示する情報：現場名、住所、連絡先、業者、日程、金額（項目4の主要なもの）
- カードタップで詳細・編集画面へ遷移
- 画面下部などに「新規案件を追加」ボタン

（モックアップイメージ：iPhone幅を想定し、PC的な7列並列カンバンではなく「タブ切り替え＋縦リスト」型のUIとしてすでに簡易プロトタイプ確認済み。この方向で実装してよい）

### 4.2 案件詳細・編集画面
- 項目3の全データを表示・編集可能
- ステータスを変更できるUI（ドロップダウン等）
- 削除機能もあると望ましい

### 4.3 新規案件登録画面
- 項目3の入力フォーム（初期ステータスは「見積もり依頼受付」）

## 5. 通知機能（将来対応の前提でよい）
- 現時点ではリマインダー通知は不要
- 将来的にLINE通知を追加したい意向あり
- そのため、データ構造上「次にやるべきアクションの予定日時」を持たせておくなど、後から通知機能を足しやすい設計にしておくと望ましい（今回のMVPで通知自体の実装は不要）

## 6. 金額・損益管理
- 原価（業者への支払額）と売上（顧客への提示額）を分けた管理・自動計算は不要
- 金額は合計額のみをメモ的に記録できればよい

## 7. 技術方針（Claude Codeでの検討事項）
- iPhone中心の利用のため、レスポンシブなWebアプリ（PWA化してホーム画面に追加できる形）を軸に検討
- データの永続化方法（バックエンド／DB）は、個人利用規模であることを踏まえ、シンプルな構成を推奨（例：Supabase、Firebase等の軽量バックエンドサービス）
- 将来のLINE通知連携を見据え、サーバーサイドで日時をチェックできる構成にしておくと拡張しやすい
- 認証機能（ログイン）は必須ではないが、個人のスマホでのみ使う想定であれば簡易的なもので可

## 8. 今回のMVPで対応しないこと（明示的に対象外）
- 複数ユーザー・権限管理
- 業者マスタ管理（都度手入力でよい）
- 写真・書類添付
- 原価と売上を分けた損益自動計算
- プッシュ通知・LINE通知の実装（設計だけ考慮）
